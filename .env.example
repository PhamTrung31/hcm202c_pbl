# .env.example — copy to .env for local development and fill values
# Do NOT commit real secrets. Use hosting provider env settings (Render, Vercel, Netlify)

# Public (safe to commit)
VITE_PUBLIC_BUILDER_KEY=__BUILDER_PUBLIC_KEY__
PING_MESSAGE="ping pong"

# Secrets (leave empty in .env and set in Render dashboard)
MONGODB_CONNECTION_STRING=
GEMINI_API_KEY=

# Example Render notes:
# - Set the environment variables in Render > Service > Environment > Environment
# - Build command: pnpm run build
# - Start command: pnpm start
# - Ensure Node version is compatible (Node 18+ recommended for ESM)
