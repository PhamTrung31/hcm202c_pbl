# .env — local configuration. IMPORTANT: do NOT commit secrets to the repo.
# Move any secret values (database URIs, API keys) to your hosting provider's
# environment variables (Render dashboard, etc.) or use a secret store.

# Public (non-secret) values
VITE_PUBLIC_BUILDER_KEY=__BUILDER_PUBLIC_KEY__
PING_MESSAGE="ping pong"

# Secrets: leave these blank locally and configure them in your host's env
# Example (do NOT put real secrets here):
MONGODB_CONNECTION_STRING=
GEMINI_API_KEY=